---
label: desc
hide_body: false
fields:
- name: body
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Body
- name: other
  type: blocks
  label: Other
  template_types:
  - text-column
  config:
    min: 
    max: 
